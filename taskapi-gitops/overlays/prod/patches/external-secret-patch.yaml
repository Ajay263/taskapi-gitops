apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: taskapi-secrets
spec:
  data:
    - secretKey: db-password
      remoteRef:
        key: taskapi/prod
        property: db-password
    - secretKey: api-key
      remoteRef:
        key: taskapi/prod
        property: api-key