---
- name: Add Argo Helm repo
  ansible.builtin.command: helm repo add argo https://argoproj.github.io/argo-helm
  changed_when: false

- name: Update Helm repos
  ansible.builtin.command: helm repo update
  changed_when: false

- name: Install ArgoCD via Helm
  ansible.builtin.shell: |
    for i in 1 2 3; do
      helm upgrade --install argocd argo/argo-cd \
        --version {{ argocd_helm_version }} \
        --namespace argocd \
        --create-namespace \
        --set server.service.type=NodePort \
        --set server.service.nodePort=30444 \
        --set redis.enabled=true \
        --wait \
        --timeout 600s && break
      echo "Attempt $i failed, retrying in 30s..."
      sleep 30
    done
  register: argocd_helm
  changed_when: argocd_helm.rc == 0

- name: Wait for ArgoCD server
  ansible.builtin.command: >
    kubectl wait --for=condition=available deployment/argocd-server
    --namespace=argocd --timeout=300s
  changed_when: false

- name: Get ArgoCD admin password
  ansible.builtin.shell: |
    kubectl -n argocd get secret argocd-initial-admin-secret \
      -o jsonpath="{.data.password}" | base64 -d
  register: argocd_password
  changed_when: false

- name: Show ArgoCD admin password
  ansible.builtin.debug:
    msg: "ArgoCD admin password: {{ argocd_password.stdout }}"

- name: Add GitOps repository to ArgoCD via kubectl secret
  ansible.builtin.shell: |
    kubectl create secret generic argocd-repo-taskapi-gitops \
      --namespace argocd \
      --from-literal=type=git \
      --from-literal=url={{ argocd_repo_url }} \
      --from-literal=username=Ajay263 \
      --from-literal=password={{ argocd_repo_token }} \
      --dry-run=client -o yaml | \
    kubectl label --local -f - \
      argocd.argoproj.io/secret-type=repository \
      --dry-run=client -o yaml | \
    kubectl apply -f -
  changed_when: true
  no_log: true

- name: Create required namespaces
  ansible.builtin.command: "kubectl create namespace {{ item }}"
  loop:
    - taskapi-dev
    - taskapi-staging
    - taskapi-prod
  register: ns_create
  changed_when: ns_create.rc == 0
  failed_when: ns_create.rc != 0 and "already exists" not in ns_create.stderr

- name: Apply ArgoCD AppProject
  ansible.builtin.command: >
    kubectl apply -f {{ role_path }}/../../../argocd/projects/taskapi-project.yaml
  changed_when: false

- name: Patch AppProject to allow ClusterSecretStore
  ansible.builtin.shell: |
    kubectl patch appproject taskapi -n argocd --type=json -p='[
      {"op":"add","path":"/spec/clusterResourceWhitelist/-",
       "value":{"group":"external-secrets.io","kind":"ClusterSecretStore"}}
    ]'
  changed_when: false
  failed_when: false

- name: Apply ArgoCD application
  ansible.builtin.command: >
    kubectl apply -f {{ role_path }}/../../../argocd/apps/taskapi-dev.yaml
  changed_when: false

- name: Wait for ArgoCD to sync
  ansible.builtin.command: >
    kubectl wait --for=jsonpath='{.status.sync.status}'=Synced
    application/taskapi-dev -n argocd --timeout=120s
  changed_when: false
  failed_when: false

- name: Show ArgoCD app status
  ansible.builtin.command: >
    kubectl get application taskapi-dev -n argocd
    -o jsonpath="{.status.health.status} {.status.sync.status}"
  register: argocd_app_status
  changed_when: false
  failed_when: false

- name: Display ArgoCD status
  ansible.builtin.debug:
    msg: "ArgoCD taskapi-dev: {{ argocd_app_status.stdout }}"
